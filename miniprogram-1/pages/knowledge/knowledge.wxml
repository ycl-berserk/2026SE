<view class="page">
  <view class="search-card">
    <view class="search-title">Knowledge Base</view>
    <view class="search-desc">Reads article and template data from the backend.</view>
    <input
      class="search-input"
      placeholder="Search process, certificate, leave..."
      bindinput="onKeywordInput"
    />
  </view>

  <scroll-view class="chips-scroll" scroll-x>
    <view class="chips-row">
      <view
        wx:for="{{categories}}"
        wx:key="*this"
        class="chip {{selectedCategory === item ? 'chip-active' : ''}}"
        data-category="{{item}}"
        bindtap="onCategoryTap"
      >
        {{item}}
      </view>
    </view>
  </scroll-view>

  <view class="section">
    <view class="section-title">Articles</view>
    <view class="section-desc">Current demo shows title and summary only.</view>
    <view wx:if="{{visibleArticles.length}}" class="article-list">
      <view wx:for="{{visibleArticles}}" wx:key="id" class="article-card" data-id="{{item.id}}" bindtap="onArticleTap">
        <view class="article-head">
          <view class="article-category">{{item.category}}</view>
          <view class="article-source">{{item.source}}</view>
        </view>
        <view class="article-title">{{item.title}}</view>
        <view class="article-summary">{{item.summary}}</view>
        <view class="article-answer">{{item.answer}}</view>
      </view>
    </view>
    <view wx:else class="empty-card">No matched result.</view>
  </view>

  <view class="section section-last">
    <view class="section-title">Templates</view>
    <view class="section-desc">Current demo lists template metadata only.</view>
    <view class="template-list">
      <view
        wx:for="{{templates}}"
        wx:key="id"
        class="template-card"
        data-name="{{item.name}}"
        data-file-id="{{item.fileId}}"
        bindtap="onTemplateTap"
      >
        <view>
          <view class="template-name">{{item.name}}</view>
          <view class="template-desc">{{item.desc}}</view>
        </view>
        <view class="template-format">{{item.format}}</view>
      </view>
    </view>
  </view>
</view>
