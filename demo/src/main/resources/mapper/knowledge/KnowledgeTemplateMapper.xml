<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruc.platform.knowledgeness.mapper.KnowledgeTemplateMapper">

    <!-- 查询启用的模板列表 -->
    <select id="selectEnabledTemplates" resultType="com.ruc.platform.knowledgeness.entity.KnowledgeTemplate">
        SELECT
            id,
            name,
            description,
            category,
            file_id,
            format,
            download_count,
            status,
            created_at,
            updated_at
        FROM knowledge_template
        WHERE status = 1
        ORDER BY created_at DESC
    </select>

</mapper>
